import React from 'react'
// import PropTypes from 'prop-types'

/**
 * @summary ListWordsPresentation
 * @version 1.0.1
 * @component
 *
 * @param {object} props
 *
 * @returns {node} jsx markup
 */
function ListWordsPresentation() {
  return <div>ListWordsPresentation</div>
}
// PROPTYPES
// const { string } = PropTypes
ListWordsPresentation.propTypes = {
  //   something: string,
}

export default ListWordsPresentation
